import styled from "styled-components";

export const LinesStyle = styled.div`
  div.text-line {
    display: flex;
    margin-bottom: 37px;

    hr.line-one {
      border: 2px solid var(--red);
      width: 57px;
    }
    hr.line-two {
      border: 2px solid var(--red);
      width: 8px;
      margin-left: 5px;
    }
    hr.line-three {
      border: 2px solid var(--red);
      width: 4px;
      margin-left: 3px;
    }
  }
`;
